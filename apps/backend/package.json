{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js --sourcemap",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "postinstall": "npx prisma generate --schema=../../packages/db/prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.12.1",
    "express": "^4.17.1",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "esbuild": "^0.23.0",
    "nodemon": "^3.1.0",
    "prisma": "^5.12.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "prisma": {
    "schema": "../../packages/db/prisma/schema.prisma",
    "dotenv": "../../packages/db/prisma/.env"
  }
}
